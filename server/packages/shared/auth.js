//? Authentication MiddleWare
import { Forbidden } from "./errors.js"; //if you're export multiple file then u have to use the same syntax as that
import express from 'express'
const authenticator = (roles) => {
 
    

};

